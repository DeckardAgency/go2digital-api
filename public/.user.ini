; Per-directory PHP settings, read by PHP-FPM (including Cloudways).
; post_max_size must be >= upload_max_filesize; keep a small margin for form fields.
upload_max_filesize = 10M
post_max_size = 12M
